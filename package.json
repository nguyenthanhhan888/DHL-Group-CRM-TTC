{
  "name": "kiosk-crm-v1",
  "private": true,
  "scripts": {
    "build": "node scripts/generate-config.mjs",
    "test": "node --test tests/*.test.*"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@electric-sql/pglite": "0.5.8"
  }
}
